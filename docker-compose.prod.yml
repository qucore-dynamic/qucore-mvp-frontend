services:
  qucore_mvp_frontend:
    build: .
    env_file:
      - .env
      - .env.prod
    container_name: client
    ports:
      - '3000:3000'
    networks: [qucore_mvp_network]

networks:
  qucore_mvp_network:
    external: true
